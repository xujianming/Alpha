#pragma once
/*
#include <map>
#include <string>

class CCamera;

class CScene
{
public:
	CScene();
	virtual ~CScene();
	
	CCamera* CreateMainCamera();
	CCamera* GetCameraByName(const char* cameraName);
	CCamera* CreateCamera(const char* cameraName);
private:
	std::map<std::string, CCamera*> m_mapCamera;
};*/